{
  "name": "comp_main",
  "runtime": "nodejs",
  "custom": {
    "optimize": {
      "exclude": [ "aws-sdk" ],
      "transforms": [{
        "name": "babelify",
        "opts": {
          "presets": ["es2015", "stage-2", "react"]
        }
      }]
    }
  }
}